<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.profit.persist.mapper.StockMapper">

    <select id="selectAll" resultType="org.profit.persist.domain.Stock">
        select * from stock
    </select>

    <select id="select" resultType="org.profit.persist.domain.Stock">
        select * from stock where code=#{code}
    </select>

    <insert id="insert">
		insert into stock (
			code, name, downloadedBase, downloadedFund, analyticed
		) values (
			#{code}, #{name}, #{downloadedBase}, #{downloadedFund}, #{analyticed}
		)
	</insert>

    <update id="update">
		update stock set downloadedBase=#{downloadedBase}, downloadedFund=#{downloadedFund}, analyticed=#{analyticed}, name=#{name} where code=#{code}
	</update>

</mapper>