<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.profit.persist.mapper.sys.UserMapper">

    <select id="login" resultType="org.profit.persist.domain.sys.User">
		select * from sys_user where account=#{account} and password=#{password}
	</select>

	<insert id="insert">
		insert into sys_user (
			account, password
		) values (
			#{account}, #{password}
		)
	</insert>

    <update id="update">
		update sys_user set password=#{password} where account=#{account}
	</update>

</mapper>